# Half-strain error charts

`analyze.py` runs the shared script in `examples/heldout_genera/ds/`. Charts and `half_strains_findings.md` for this community are written here. The held-out and samovar10 notes stay in the other `ds/` folder.
