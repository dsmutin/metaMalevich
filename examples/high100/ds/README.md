# Family read-fill scores for high100

`score_family.py` writes `family_metrics.tsv` for the 100000-read community. Neighbour rows copy a family along MEGAHIT overlap edges. The 4-mer rows use the nearest Kraken-labelled final contig.
