# Half of low75, scored at genus

Tables and charts are written here by `examples/low75/ds/genus_graph.py`. The 4-mer graph is the same rule as on the full 75-genus community. `initial_graph.py` and `edge_4mer.py` copy their tables here as well.
